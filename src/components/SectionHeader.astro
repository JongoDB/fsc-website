---
interface Props {
  title: string;
  description?: string;
  class?: string;
}

const { title, description, class: className = '' } = Astro.props;
---

<div class={`section-header scroll-animate ${className}`}>
  <h2>{title}</h2>
  {description && <p>{description}</p>}
</div>
