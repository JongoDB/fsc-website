---
interface Props {
  role: string;
  title: string;
  bio: string;
}

const { role, title, bio } = Astro.props;
---

<div class="leadership-card">
  <h3>{role}</h3>
  <p class="leadership-title">{title}</p>
  <p class="leadership-bio">{bio}</p>
</div>
